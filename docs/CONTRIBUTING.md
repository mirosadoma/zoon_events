# Contributing

Read the constitution, spec, plan, contracts, and current tasks. Add tests before behavior,
keep module boundaries, use synthetic data, update OpenAPI/docs, and run:
`composer quality`, `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`.
Never add Docker/Sail, secrets, licensed assets, or excluded product modules in Phase 0.
